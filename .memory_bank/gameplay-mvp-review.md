# Ревью Gameplay MVP Roadmap

**Дата:** 29 декабря 2025  
**Ветка:** `copilot/review-gameplay-mvp-roadmap`  
**Базовый план:** `docs/SlimeArena-Plan-v1.8.md`, раздел 3.2 "Внутренний MVP (Gameplay MVP)"

---

## 1. Резюме

Проведён тщательный анализ текущей реализации проекта относительно требований Gameplay MVP из дорожной карты. Цель: идентифицировать реализованные компоненты, обнаружить недостающие элементы и составить пошаговый план реализации.

**Общий статус:** ~65% Gameplay MVP реализовано.

---

## 2. Детальный анализ по задачам

### 2.1. ✅ 3 класса (пассивы + стартовое умение)

**Статус:** РЕАЛИЗОВАНО

**Что сделано:**
- 3 класса определены в `config/balance.json`: hunter, warrior, collector
- Пассивные характеристики применяются через `getClassStats()`:
  - Hunter: speedMult 1.15, hpMult 0.9
  - Warrior: speedMult 0.9, hpMult 1.15, damageVsSlimeMult 1.1
  - Collector: radiusMult 1.25, eatingPowerMult 1.15
- Различия в `swallowLimit` и `biteFraction` между классами
- Стартовые умения привязаны к классам (неявно через UI)

**Файлы:**
- `config/balance.json` (классы определены)
- `server/src/rooms/ArenaRoom.ts` (getClassStats, getSlimeConfig)
- `client/src/main.ts` (иконки классов: 🔥 👊 💎)

---

### 2.2. ✅ Умения MVP: Рывок (Dash)

**Статус:** РЕАЛИЗОВАНО (PR #6)

**Что сделано:**
- Серверная логика `activateDash()`: линейное движение к цели за 0.3 сек
- Стоимость 3% массы, кулдаун 5 сек, дистанция 80 м
- FLAG_DASHING синхронизируется с клиентом
- Визуал: огненный след (5 затухающих кругов + хвост)
- UI: кнопка способности с SVG прогресс-кругом кулдауна
- Горячая клавиша: 1 (slot 0)

**Файлы:**
- `server/src/rooms/ArenaRoom.ts` (activateDash, физика рывка)
- `client/src/main.ts` (dash trail визуал, UI кнопка)
- `config/balance.json` (abilities.dash)
- `shared/src/constants.ts` (FLAG_DASHING)

---

### 2.3. ❌ Умения MVP: Выброс (Projectile)

**Статус:** НЕ РЕАЛИЗОВАНО

**Что отсутствует:**
- Нет схемы `Projectile` в GameState
- Нет серверной логики запуска снаряда
- Нет физики движения снаряда (линейное движение 400 м/сек)
- Нет детекции попадания по слаймам
- Нет визуализации снаряда на клиенте
- Нет конфига в `balance.json` для projectile ability

**Требования из GDD:**
- Дальность: 300 м
- Скорость: 400 м/сек
- Урон: 100% от базового урона слайма
- Проходит сквозь пузыри, но не сквозь слаймов
- Стоимость: 2% массы, кулдаун: 3 сек

---

### 2.4. ✅ Умения MVP: Щит (Shield)

**Статус:** РЕАЛИЗОВАНО (PR #6)

**Что сделано:**
- Серверная логика `activateShield()`: 3 сек неуязвимости
- FLAG_ABILITY_SHIELD блокирует урон в processCombat
- Щит снимается досрочно при атаке
- Стоимость 4% массы, кулдаун 8 сек
- Визуал: голубое свечение (круг r×1.4 с inner glow)
- UI: кнопка с иконкой 🛡️

**Файлы:**
- `server/src/rooms/ArenaRoom.ts` (activateShield, блокирование урона)
- `client/src/main.ts` (shield glow визуал)
- `config/balance.json` (abilities.shield)

---

### 2.5. ✅ Умения MVP: Притяжение (Magnet)

**Статус:** РЕАЛИЗОВАНО (PR #6)

**Что сделано:**
- Серверная логика `activateMagnet()`: притяжение орбов в радиусе 150 м
- Притягивает только орбы (не слаймов), сила 50 м/сек
- Стоимость 1.5% массы, кулдаун 6 сек, длительность 1.5 сек
- FLAG_MAGNETIZING синхронизируется
- Визуал: фиолетовый градиент + 8 магнитных линий
- UI: кнопка с иконкой 🧲

**Файлы:**
- `server/src/rooms/ArenaRoom.ts` (activateMagnet, притяжение в updateOrbs)
- `client/src/main.ts` (magnet field визуал)
- `config/balance.json` (abilities.magnet)
- `shared/src/constants.ts` (FLAG_MAGNETIZING)

---

### 2.6. ✅ GCD 100 мс + очередь нажатий (max 1)

**Статус:** РЕАЛИЗОВАНО

**Что сделано:**
- `globalCooldownMs: 100` в balance.json
- `abilityQueueSize: 1` в balance.json
- Сервер проверяет `player.gcdReadyTick` перед активацией умения
- Очередь через `player.queuedAbilitySlot` (максимум 1 слот)
- Сервер обрабатывает очередь на следующем тике после GCD

**Файлы:**
- `server/src/rooms/ArenaRoom.ts` (GCD логика в onTick)
- `config/balance.json` (server.globalCooldownMs, abilityQueueSize)

---

### 2.7. ✅ Сундуки: спавн, физика, типы, награды

**Статус:** РЕАЛИЗОВАНО (частично)

**Что сделано:**
- Схема `Chest` в GameState (x, y, vx, vy, type)
- Спавн сундуков: `spawnChest()` каждые 20 сек (maxCount: 3)
- Физика сундуков: участвуют в collisionSystem как динамические объекты
- Открытие сундука: `openChest()` при касании пастью
- Награды: 
  - 40% шанс на талант (grantTalent)
  - 60% шанс на массу (10-30% от текущей массы)
- Удаление сундука после открытия

**Что отсутствует:**
- Типы сундуков (обычный/редкий/эпический) — есть только `chest.type`, но используется для индекса массы
- Различия в наградах по типам (по GDD: обычный 70%, редкий 25%, эпический 5%)
- Гарантированный талант из редкого сундука
- Гарантированный эпический талант из эпического сундука
- Визуальное отличие типов сундуков на клиенте

**Файлы:**
- `server/src/rooms/ArenaRoom.ts` (spawnChest, openChest, collisionSystem)
- `server/src/rooms/schema/GameState.ts` (Chest class)
- `config/balance.json` (chests)
- `client/src/main.ts` (рендеринг сундуков)

---

### 2.8. ✅ Фазы матча + Hot Zones + голод

**Статус:** РЕАЛИЗОВАНО

**Что сделано:**
- Фазы матча: Spawn (0-15с), Collect (15-60с), Hunt (60-90с), Chaos (90-120с), Final (120-150с)
- Переключение фаз в `onTick()` с вызовом `handlePhaseChange()`
- Hot Zones:
  - Спавн в фазе Chaos: 2 зоны (spawnMultiplier: 3)
  - Спавн в фазе Final: 1 зона в центре (spawnMultiplier: 5)
  - Радиус зоны: 110 м
  - Визуализация на клиенте: красные полупрозрачные круги
- Голод (hunger system):
  - Активен для всех живых игроков
  - Базовая потеря: 2 кг/сек
  - Масштабируется с массой: +0.01 кг/сек за каждые 100 кг
  - Максимум: 12 кг/сек
  - Минимум: 50 кг (не может умереть от голода)

**Файлы:**
- `server/src/rooms/ArenaRoom.ts` (handlePhaseChange, spawnHotZones, hungerSystem)
- `server/src/rooms/schema/GameState.ts` (HotZone class, phase field)
- `config/balance.json` (match.phases, hotZones, hunger)
- `client/src/main.ts` (рендеринг hot zones)

---

### 2.9. ✅ Механика «Последний вздох»

**Статус:** РЕАЛИЗОВАНО

**Что сделано:**
- Длительность: 0.5 сек неуязвимости после смерти
- Флаг `player.isLastBreath = true`
- Штраф скорости: 80% (lastBreathSpeedPenalty: 0.8)
- Применяется к линейной и угловой скорости в FlightAssist
- После окончания Last Breath игрок финализирует смерть
- Визуал на клиенте: текстовый индикатор "LB" отображается под именем игрока

**Что отсутствует:**
- Визуальный эффект мигания/прозрачности на клиенте (может быть добавлен позже для лучшей видимости)

**Файлы:**
- `server/src/rooms/ArenaRoom.ts` (Last Breath логика в processCombat, deathSystem, flightAssistSystem)
- `config/balance.json` (combat.lastBreathDurationSec, lastBreathSpeedPenalty)
- `client/src/main.ts` (строка 4389: отображение индикатора "LB")

---

### 2.10. ✅ Система «Мятежник» (Rebel)

**Статус:** РЕАЛИЗОВАНО

**Что сделано:**
- Автоматическое назначение лидера как Мятежника
- Обновление каждые 5 сек (rebel.updateIntervalSec)
- Порог назначения: 1.2× средняя масса (massThresholdMultiplier: 1.2)
- Награда за убийство Мятежника: 10% массы убитого
- Синхронизация через `state.rebelId`
- Визуальный маркер на клиенте: 👑 перед именем, жёлтый цвет имени
- Показ короны в лидерборде и Results screen

**Файлы:**
- `server/src/rooms/ArenaRoom.ts` (Rebel логика в updateLeaderboard, processCombat)
- `server/src/rooms/schema/GameState.ts` (rebelId field)
- `config/balance.json` (rebel)
- `client/src/main.ts` (рендеринг короны, жёлтый цвет)

---

### 2.11. ⚠️ Таланты: очередь (max 3) + быстрый выбор

**Статус:** ЧАСТИЧНО РЕАЛИЗОВАНО

**Что сделано:**
- Очередь талантов: `player.talentsAvailable` (max 3)
- Выбор таланта: 3 фиксированных варианта (Mass Surge +5%, Mass Boost +30%, Guard Pulse +3% + щит)
- Автовыбор через 30 сек (TALENT_AUTO_SELECT_MS = 30000)
- UI: модальное окно с выбором талантов
- Применение эффектов: `applyTalentChoice()` на сервере
- Быстрый выбор: клавиши 1/2/3 или клик на кнопку

**Что отсутствует:**
- **Пул из 30 талантов** (15 обычных, 10 редких, 5 эпических) — сейчас только 3 фиксированных
- **Система редкости** (60% обычный, 30% редкий, 10% эпический) — нет рандомизации
- **Разнообразные эффекты талантов** (см. GDD 7.5):
  - Живучесть: Толстая кожа, Регенерация, Упорство, и т.д.
  - Манёвренность: Проворность, Лёгкость, Экономия, и т.д.
  - Урон: Острые зубы, Силовой приём, Ядовитый укус, и т.д.
  - Сбор: Магнетизм, Обжора, Жадность, Предчувствие, и т.д.
  - Универсал: Ускорение КД, Экономия массы, и т.д.
- **Приоритет автовыбора по классам** — сейчас случайный или по индексу
- **Стековые эффекты талантов** (например, +10% HP можно взять несколько раз)
- **Хранение выбранных талантов** — нет списка активных талантов у игрока
- **Отмена режима выбора при получении урона** — не реализовано (есть TODO в GDD 7.3)

**Файлы:**
- `server/src/rooms/ArenaRoom.ts` (applyTalentChoice, grantTalent)
- `server/src/rooms/schema/GameState.ts` (talentsAvailable field)
- `client/src/main.ts` (talent modal UI, автовыбор)
- `shared/src/types.ts` (InputCommand.talentChoice)

---

### 2.12. ❌ Таланты: минимальный пул (15 common) + автовыбор по классу

**Статус:** НЕ РЕАЛИЗОВАНО

**Связано с:** Задача 2.11 (расширение системы талантов)

**Что отсутствует:**
- Полный пул талантов из 30 штук (см. GDD 7.5)
- Система редкости с вероятностями
- Приоритеты автовыбора по классам (Hunter: Манёвренность > Урон, и т.д.)
- Хранение и применение разнообразных эффектов

---

### 2.13. ✅ HUD: масса/время/топ-5

**Статус:** РЕАЛИЗОВАНО

**Что сделано:**
- HUD показывает:
  - Моя масса: X кг
  - Время: MM:SS
  - Лидеры: топ-5 с массой и позицией
  - Корона 👑 для Мятежника
  - Метка (you) для локального игрока
  - Таланты: N (если доступны)

**Файлы:**
- `client/src/main.ts` (HUD рендеринг)

---

### 2.14. ✅ HUD: кнопки умений + индикаторы КД/GCD

**Статус:** РЕАЛИЗОВАНО (PR #6)

**Что сделано:**
- Кнопка способности (70×70 px, правый нижний угол)
- Иконки: ⚡ (Dash), 🛡️ (Shield), 🧲 (Magnet)
- Подпись "1" (горячая клавиша)
- SVG прогресс-круг для кулдауна
- Тёмный оверлей 80% на кулдауне
- Яркое голубое свечение когда готово (#64dcff рамка)
- Таймер секунд с десятыми (например "5.2")

**Что отсутствует:**
- Индикатор GCD (100 мс) — не визуализируется отдельно, так как очень короткий

**Файлы:**
- `client/src/main.ts` (ability button UI, updateAbilityCooldown)

---

### 2.15. ❌ HUD: индикатор очереди талантов + быстрый выбор

**Статус:** ЧАСТИЧНО РЕАЛИЗОВАНО

**Что сделано:**
- Модальное окно выбора талантов открывается автоматически при `talentsAvailable > 0`
- Таймер автовыбора отображается в модалке
- Счётчик доступных талантов в HUD: "Таланты: N"

**Что отсутствует:**
- Визуальный индикатор "+N" возле кнопок умений (как в GDD 7.2)
- Возможность открыть выбор по нажатию на "+N"
- Режим быстрого выбора: заменить кнопки умений на таланты (по GDD 7.2)
- Отмена выбора свайпом вниз
- Закрытие режима выбора при получении урона

**Файлы:**
- `client/src/main.ts` (talent modal)

---

### 2.16. ✅ HUD: маркер Мятежника + события (toasts)

**Статус:** ЧАСТИЧНО РЕАЛИЗОВАНО

**Что сделано:**
- Маркер Мятежника: 👑 перед именем, жёлтый цвет
- Показ в HUD лидерборда
- Показ в Results screen

**Что отсутствует:**
- События (toasts) при назначении/убийстве Мятежника
- Уведомления в игре (например, "Вы стали Мятежником!", "Мятежник убит!")

**Файлы:**
- `client/src/main.ts` (рендеринг маркера)

---

### 2.17. ⚠️ Визуал Last Breath (мигание/прозрачность)

**Статус:** НЕ РЕАЛИЗОВАНО

**Что отсутствует:**
- Визуальный эффект мигания игрока в Last Breath на клиенте
- Изменение прозрачности (alpha) слайма

**Файлы:**
- `client/src/main.ts` (требуется добавить визуал)

---

### 2.18. ❌ Телеметрия матчевых событий (минимум)

**Статус:** НЕ РЕАЛИЗОВАНО

**Что отсутствует:**
- События из Plan v1.8.2, раздел 5.1:
  - `match_start`, `match_end`
  - `player_death`, `last_breath_started`
  - `rebel_assigned`, `rebel_killed`
  - `orb_eaten`, `ability_used`
  - `chest_opened`, `talent_chosen`
- Логирование событий на сервере
- Отправка событий в аналитическую систему (опционально)

**Файлы:**
- Требуется создать модуль телеметрии

---

### 2.19. ⚠️ Инструментальный конфиг баланса (json + hot reload в dev)

**Статус:** ЧАСТИЧНО РЕАЛИЗОВАНО

**Что сделано:**
- Баланс хранится в `config/balance.json`
- Загрузка через `loadBalanceConfig()` из `server/src/config/loadBalanceConfig.ts`
- Валидация схемы через `shared/src/config.ts` (BalanceConfig type)

**Что отсутствует:**
- Hot reload в dev режиме (watch balance.json и перезагрузка без перезапуска сервера)
- Web UI для редактирования баланса (опционально)

**Файлы:**
- `config/balance.json`
- `server/src/config/loadBalanceConfig.ts`
- `shared/src/config.ts`

---

## 3. Общая картина: что реализовано

### ✅ Полностью реализовано (11 задач)

1. 3 класса (пассивы + стартовое умение)
2. Умения MVP: Рывок (Dash)
3. Умения MVP: Щит (Shield)
4. Умения MVP: Притяжение (Magnet)
5. GCD 100 мс + очередь нажатий (max 1)
6. Фазы матча + Hot Zones + голод
7. Механика «Последний вздох»
8. Система «Мятежник» (Rebel)
9. HUD: масса/время/топ-5
10. HUD: кнопки умений + индикаторы КД
11. Сундуки: базовая механика (спавн, физика, открытие)

### ⚠️ Частично реализовано (5 задач)

1. **Сундуки**: базовая механика есть, но отсутствуют типы сундуков (обычный/редкий/эпический) и различия в наградах
2. **Таланты: очередь + быстрый выбор**: базовая система есть, но только 3 фиксированных таланта, нет пула из 30
3. **HUD: индикатор очереди талантов**: показывает счётчик, но нет "+N" индикатора и режима быстрого выбора
4. **HUD: маркер Мятежника**: показывает маркер, но нет событий (toasts)
5. **Инструментальный конфиг баланса**: JSON есть, hot reload отсутствует

### ❌ Не реализовано (3 задачи)

1. **Умения MVP: Выброс (Projectile)** — критическая задача
2. **Таланты: минимальный пул (15 common)** — критическая задача
3. **Визуал Last Breath (мигание/прозрачность)** — некритично для MVP
4. **Телеметрия матчевых событий** — некритично для внутреннего MVP, но важно для анализа

---

## 4. Критические недостатки для Gameplay MVP

### 4.1. Отсутствие Выброса (Projectile)

**Критичность:** ВЫСОКАЯ

**Причина:**
- Выброс — одно из 4 MVP-умений согласно GDD
- Единственное дальнобойное умение
- Важно для баланса классов (особенно для Hunter)
- Без него геймплей становится однообразным (только ближний бой)

### 4.2. Ограниченный пул талантов

**Критичность:** ВЫСОКАЯ

**Причина:**
- Сейчас только 3 фиксированных таланта (повторяющийся выбор скучен)
- По плану MVP требуется минимум 15 обычных талантов
- Разнообразие талантов — ключевая прогрессия в матче
- Отсутствие системы редкости снижает интерес

### 4.3. Отсутствие типов сундуков

**Критичность:** СРЕДНЯЯ

**Причина:**
- Сундуки — важный источник талантов и массы
- Типы сундуков (обычный/редкий/эпический) создают момент «лута»
- Без различий сундуки теряют интерес

### 4.4. Отсутствие телеметрии

**Критичность:** СРЕДНЯЯ (для внутреннего тестирования)

**Причина:**
- Без телеметрии невозможно анализировать баланс
- Плейтесты требуют данных для итераций
- Критерии успеха MVP (средняя длительность матча 2-3 мин, доля убийств Мятежника ≥1) невозможно измерить

---

## 5. Выводы

### Сильные стороны текущей реализации

1. **Солидная основа:** Core Prototype завершён полностью, физика стабильна
2. **3 из 4 умений готовы:** Dash, Shield, Magnet работают стабильно с хорошей визуализацией
3. **Фазы и Hot Zones:** динамика матча реализована, финал в центре зоны создаёт напряжение
4. **Система Мятежника:** работает корректно, создаёт интересную цель
5. **Last Breath:** даёт шанс на контратаку, реализована механически

### Что требует доработки для завершения MVP

1. **Реализовать Выброс (Projectile)** — критично
2. **Расширить пул талантов до 30** — критично
3. **Добавить типы сундуков** — важно для разнообразия
4. **Добавить телеметрию** — важно для тестирования
5. **Улучшить UI талантов** — индикатор "+N", режим быстрого выбора
6. **Добавить визуал Last Breath** — некритично, но улучшает UX
7. **Добавить события (toasts)** — Мятежник, таланты, и т.д.

### Оценка готовности к плейтестам

**Текущее состояние:** Можно начинать **ограниченное внутреннее тестирование** с упрощённой механикой (3 таланта, без Выброса).

**Для полноценных плейтестов необходимо:**
- Реализовать Выброс (блокирующая задача)
- Расширить пул талантов до 15+ (блокирующая задача)
- Добавить телеметрию (блокирующая для анализа)

---

## 6. Рекомендации

### Приоритет 1 (критические задачи)

1. **Реализовать Выброс (Projectile)** — 3-4 дня
2. **Создать пул из 30 талантов с редкостью** — 5-6 дней
3. **Добавить телеметрию матчевых событий** — 3 дня

### Приоритет 2 (важные улучшения)

4. **Типы сундуков (обычный/редкий/эпический)** — 2 дня
5. **Индикатор "+N" талантов и режим быстрого выбора** — 2 дня
6. **События (toasts) для Мятежника и талантов** — 2 дня

### Приоритет 3 (некритичные улучшения)

7. **Визуал Last Breath (мигание/прозрачность)** — 1 день
8. **Hot reload баланса в dev** — 2 дня

**Итоговая оценка до Gameplay MVP Ready:** 18-22 дня (приоритеты 1-2)

---

## 7. Заключение

Проект находится в хорошем состоянии: Core Prototype завершён, 3 из 4 MVP-умений работают стабильно, фазы и системы (Rebel, Last Breath, Hunger) реализованы. 

**Основные блокеры для Gameplay MVP:**
- Выброс (Projectile) — критическое умение
- Расширение пула талантов — ключевая прогрессия

После завершения этих двух задач + добавления телеметрии проект будет готов к полноценным внутренним плейтестам.

**Рекомендация:** Сфокусироваться на Приоритете 1 (Выброс, Таланты, Телеметрия) в первую очередь.
