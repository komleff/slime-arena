# ===========================================
# Production Environment Variables
# COPY TO: /root/.env.production (на сервере)
# DO NOT commit this file with real values!
# ===========================================

# ===========================================
# JWT & Security
# ===========================================
# Минимум 32 символа, криптографически стойкая строка
JWT_SECRET="your-production-jwt-secret-min-32-characters"

# Токен для связи между Match Server и Meta Server
MATCH_SERVER_TOKEN="your-match-server-token-min-32-characters"

# Время жизни claim токена (в минутах)
CLAIM_TOKEN_TTL_MINUTES=60

# ===========================================
# OAuth — Yandex
# ===========================================
# Получить на: https://oauth.yandex.ru/client/new
YANDEX_CLIENT_ID="your-yandex-client-id"
YANDEX_CLIENT_SECRET="your-yandex-client-secret"
OAUTH_YANDEX_ENABLED=true

# ===========================================
# Database & Cache
# ===========================================
# Контейнер использует локальные 127.0.0.1
# Не менять если PostgreSQL и Redis в одном контейнере
# DATABASE_URL=postgresql://slime:slime_dev_password@127.0.0.1:5432/slime_arena
# REDIS_URL=redis://127.0.0.1:6379

# ===========================================
# Environment
# ===========================================
# production или development
# NODE_ENV=production

# Ports (если нужно переопределить, иначе defaults внутри контейнера)
# PORT=2567
# META_PORT=3000
