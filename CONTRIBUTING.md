# Руководство по внесению вклада в проект Slime Arena

Спасибо за интерес к проекту Slime Arena! Мы рады любому вкладу в развитие игры.

## Содержание

- [Кодекс поведения](#кодекс-поведения)
- [Как я могу помочь?](#как-я-могу-помочь)
- [Процесс разработки](#процесс-разработки)
- [Структура проекта](#структура-проекта)
- [Стандарты кода](#стандарты-кода)
- [Коммиты и Pull Request](#коммиты-и-pull-request)
- [Тестирование](#тестирование)
- [Сообщения об ошибках](#сообщения-об-ошибках)
- [Предложения новых функций](#предложения-новых-функций)

## Кодекс поведения

### Наши обязательства

В интересах создания открытой и дружелюбной атмосферы мы, как участники и поддерживающие проект, обязуемся сделать участие в нашем проекте и сообществе комфортным для всех, независимо от возраста, размера тела, инвалидности, этнической принадлежности, гендерной идентичности и выражения, уровня опыта, образования, социально-экономического статуса, национальности, внешности, расы, религии или сексуальной идентичности и ориентации.

### Ожидаемое поведение

- Использование приветливого и инклюзивного языка
- Уважительное отношение к различным точкам зрения и опыту
- Конструктивная критика
- Фокус на том, что лучше для сообщества
- Проявление эмпатии к другим участникам сообщества

## Как я могу помочь?

### Варианты участия

1. **Разработка функционала** - добавление новых игровых механик, врагов, уровней
2. **Исправление багов** - поиск и устранение ошибок в коде
3. **Улучшение производительности** - оптимизация игрового движка
4. **Тестирование** - написание и улучшение тестов
5. **Документация** - улучшение README, комментариев в коде, wiki
6. **Игровой дизайн** - предложения по геймплею и балансу
7. **Арт и звук** - создание спрайтов, анимаций, звуковых эффектов
8. **Переводы** - локализация игры на другие языки

### Первые шаги для новичков

Ищите Issues с метками:
- `good first issue` - хорошо для первого вклада
- `help wanted` - требуется помощь
- `beginner friendly` - подходит для начинающих

## Процесс разработки

### 1. Настройка окружения

```bash
# Клонировать репозиторий
git clone https://github.com/komleff/slime-arena.git
cd slime-arena

# Установить зависимости (для Node.js проекта)
npm install

# Или для Python проекта
pip install -r requirements.txt

# Запустить в режиме разработки
npm run dev
# или
python -m slime_arena
```

### 2. Создание ветки

```bash
# Создать новую ветку от main
git checkout -b feature/my-awesome-feature
# или для багфиксов
git checkout -b bugfix/fix-player-collision
```

### 3. Разработка

- Пишите чистый, читаемый код
- Следуйте существующему стилю кодирования
- Добавляйте комментарии к сложной логике
- Пишите тесты для нового функционала
- Регулярно делайте коммиты

### 4. Тестирование

```bash
# Запустить все тесты
npm test
# или
pytest

# Запустить линтеры
npm run lint
# или
pylint slime_arena/

# Проверить покрытие тестами
npm run coverage
# или
pytest --cov=slime_arena
```

### 5. Отправка Pull Request

1. Убедитесь, что все тесты проходят
2. Обновите документацию при необходимости
3. Отправьте изменения в свой fork
4. Создайте Pull Request в основной репозиторий
5. Опишите изменения подробно
6. Дождитесь ревью кода

## Структура проекта

```
slime-arena/
├── src/                    # Исходный код
│   ├── entities/          # Игровые объекты (Player, Enemy, etc.)
│   ├── scenes/            # Игровые сцены (Menu, Game, GameOver)
│   ├── systems/           # Игровые системы (Physics, Collision, etc.)
│   └── utils/             # Вспомогательные функции
├── assets/                # Ресурсы игры
│   ├── sprites/           # Спрайты и текстуры
│   ├── sounds/            # Звуковые эффекты
│   └── music/             # Музыка
├── tests/                 # Тесты
└── docs/                  # Документация
```

## Стандарты кода

### Стиль именования

**JavaScript/TypeScript:**
```javascript
// Классы - PascalCase
class PlayerEntity { }

// Функции и переменные - camelCase
function calculateDamage() { }
const playerHealth = 100;

// Константы - UPPER_SNAKE_CASE
const MAX_HEALTH = 100;
const GRAVITY = 9.8;
```

**Python:**
```python
# Классы - PascalCase
class PlayerEntity:
    pass

# Функции и переменные - snake_case
def calculate_damage():
    pass
player_health = 100

# Константы - UPPER_SNAKE_CASE
MAX_HEALTH = 100
GRAVITY = 9.8
```

### Комментарии

```javascript
/**
 * Рассчитывает урон с учетом защиты цели
 * @param {number} baseDamage - Базовый урон атаки
 * @param {number} targetDefense - Защита цели
 * @returns {number} Итоговый урон
 */
function calculateDamage(baseDamage, targetDefense) {
    return Math.max(1, baseDamage - targetDefense);
}
```

```python
def calculate_damage(base_damage: int, target_defense: int) -> int:
    """
    Рассчитывает урон с учетом защиты цели.
    
    Args:
        base_damage: Базовый урон атаки
        target_defense: Защита цели
        
    Returns:
        Итоговый урон (минимум 1)
    """
    return max(1, base_damage - target_defense)
```

### Форматирование кода

- Отступы: 4 пробела (Python) или 2 пробела (JavaScript)
- Максимальная длина строки: 100 символов
- Используйте линтеры для автоматической проверки

### Принципы проектирования

1. **SOLID принципы** - следуйте принципам объектно-ориентированного дизайна
2. **DRY (Don't Repeat Yourself)** - избегайте дублирования кода
3. **KISS (Keep It Simple, Stupid)** - стремитесь к простоте
4. **Разделение ответственности** - один класс/функция = одна задача

## Коммиты и Pull Request

### Формат сообщения коммита

```
<type>(<scope>): <subject>

<body>

<footer>
```

**Types:**
- `feat`: Новая функциональность
- `fix`: Исправление бага
- `docs`: Изменения в документации
- `style`: Форматирование кода (не влияет на логику)
- `refactor`: Рефакторинг кода
- `perf`: Улучшение производительности
- `test`: Добавление или изменение тестов
- `chore`: Изменения в инструментах сборки, зависимостях

**Примеры:**

```
feat(player): добавлена возможность двойного прыжка

Игрок теперь может совершить второй прыжок в воздухе.
Добавлена анимация двойного прыжка.

Closes #123
```

```
fix(collision): исправлено прохождение сквозь стены

Улучшен алгоритм обнаружения коллизий для предотвращения
прохождения игрока сквозь тонкие стены на высокой скорости.

Fixes #456
```

### Pull Request

**Название PR:**
```
[TYPE] Краткое описание изменений
```

**Описание PR должно включать:**
1. Что было сделано и почему
2. Скриншоты/GIF для визуальных изменений
3. Ссылки на связанные Issues
4. Результаты тестирования
5. Замечания для ревьюеров

**Чеклист PR:**
- [ ] Код соответствует стандартам проекта
- [ ] Все тесты проходят
- [ ] Добавлены тесты для нового функционала
- [ ] Документация обновлена
- [ ] Нет конфликтов с main веткой
- [ ] Линтеры не выдают ошибок

## Тестирование

### Требования к тестам

- Каждая новая функция должна иметь unit-тесты
- Покрытие кода: минимум 70%
- Критичные компоненты: минимум 90%

### Структура теста

```javascript
describe('PlayerEntity', () => {
    describe('takeDamage', () => {
        it('should reduce health by damage amount', () => {
            const player = new PlayerEntity();
            player.health = 100;
            player.takeDamage(30);
            expect(player.health).toBe(70);
        });

        it('should not reduce health below 0', () => {
            const player = new PlayerEntity();
            player.health = 20;
            player.takeDamage(50);
            expect(player.health).toBe(0);
        });
    });
});
```

### Запуск тестов

```bash
# Все тесты
npm test

# Конкретный файл
npm test -- entities/Player.test.js

# С покрытием
npm run test:coverage

# В watch режиме
npm test -- --watch
```

## Сообщения об ошибках

### Перед созданием Issue проверьте:

1. Issue еще не существует
2. Используете последнюю версию
3. Ошибка воспроизводится стабильно

### Шаблон Issue для багов:

```markdown
**Описание**
Краткое описание проблемы.

**Шаги для воспроизведения**
1. Запустить игру
2. Выполнить действие X
3. Наблюдать ошибку

**Ожидаемое поведение**
Что должно было произойти.

**Фактическое поведение**
Что произошло на самом деле.

**Скриншоты**
При наличии добавьте скриншоты.

**Окружение:**
- ОС: [например, Windows 10, macOS 14]
- Браузер: [например, Chrome 120] (для веб-версии)
- Версия игры: [например, 1.0.0]

**Дополнительная информация**
Логи, консольные ошибки и прочее.
```

## Предложения новых функций

### Шаблон Issue для предложений:

```markdown
**Описание функции**
Что предлагается добавить.

**Проблема, которую это решит**
Какую проблему или потребность закроет эта функция.

**Предлагаемое решение**
Как это должно работать.

**Альтернативы**
Какие есть альтернативные решения.

**Дополнительный контекст**
Скриншоты, примеры из других игр, мокапы.
```

## Ревью кода

### Как участник ревью:

- Будьте конструктивны и вежливы
- Объясняйте "почему", а не только "что"
- Предлагайте решения, а не только указывайте на проблемы
- Признавайте хорошие решения

### Как автор кода:

- Не воспринимайте критику лично
- Отвечайте на все комментарии
- Будьте открыты к предложениям
- Объясняйте ваши решения

## Лицензирование

Внося вклад в проект, вы соглашаетесь, что ваш код будет лицензирован под той же лицензией, что и проект.

## Вопросы?

Если у вас есть вопросы, не стесняйтесь:
- Открыть Issue с меткой `question`
- Связаться с maintainers проекта

---

Спасибо за ваш вклад в Slime Arena! 🎮✨
