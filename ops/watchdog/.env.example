# Slime Arena Watchdog — переменные окружения
#
# Скопируйте в .env и заполните значениями:
#   cp .env.example .env
#

# Директория для файлов-флагов взаимодействия с сервером
# Должна быть примонтирована в Docker-контейнер
SHARED_DIR=/opt/slime-arena/shared

# Имя Docker-контейнера для рестарта
CONTAINER_NAME=slime-arena

# URL для health-check сервера
# Должен возвращать 200 OK при работающем сервере
HEALTH_URL=http://127.0.0.1:3000/health

# Telegram бот для уведомлений о рестартах и ошибках
# Получить токен: @BotFather в Telegram
TELEGRAM_BOT_TOKEN=

# Chat ID для уведомлений
# Узнать ID: отправить сообщение боту, затем GET https://api.telegram.org/bot<TOKEN>/getUpdates
TELEGRAM_CHAT_ID=
