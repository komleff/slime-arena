# ТЗ: Server Monitoring Dashboard

**Версия:** 1.0  
**Дата:** 2026-02-03  
**Автор:** Архитектор  
**Статус:** Draft

---

## 1. Обзор

### 1.1 Цель

Создать веб-приложение для мониторинга и администрирования production-серверов Slime Arena с безопасным доступом, работающее на десктопе и мобильных устройствах.

### 1.2 Проблема

Сейчас мониторинг осуществляется вручную через SSH:
- `docker logs` — просмотр логов
- `docker stats` — ресурсы
- `curl /health` — проверка состояния
- Отсутствует централизованный дашборд
- Нет алертинга
- Нет истории метрик

### 1.3 Целевая аудитория

- Администраторы сервера
- Разработчики (для диагностики)

---

## 2. Функциональные требования

### 2.1 Дашборд (главная страница)

| ID | Требование | Приоритет |
|----|------------|-----------|
| F-DASH-01 | Отображение статуса серверов (online/offline/degraded) | P0 |
| F-DASH-02 | Показатели здоровья: DB status, Redis status, uptime | P0 |
| F-DASH-03 | Ресурсы контейнера: CPU %, RAM usage | P0 |
| F-DASH-04 | Активные игровые комнаты и игроки (общее кол-во) | P0 |
| F-DASH-05 | Последние события/ошибки (лента) | P1 |
| F-DASH-06 | Автообновление данных (интервал 5-10 сек) | P0 |

### 2.2 Детальный мониторинг

| ID | Требование | Приоритет |
|----|------------|-----------|
| F-MON-01 | Графики tick latency (avg/max) за последние N минут | P1 |
| F-MON-02 | История метрик (1ч, 6ч, 24ч, 7д) | P2 |
| F-MON-03 | Список активных комнат с деталями (игроки, фаза, tick) | P1 |
| F-MON-04 | Логи сервера (tail с фильтрацией по уровню) | P1 |
| F-MON-05 | Статистика API: RPS, latency, ошибки | P2 |

### 2.3 Административные действия

| ID | Требование | Приоритет |
|----|------------|-----------|
| F-ADM-01 | Кнопка "Restart Container" с подтверждением | P0 |
| F-ADM-02 | Аудит-лог всех административных действий | P0 |
| F-ADM-03 | Принудительное завершение комнаты (graceful) | P2 |
| F-ADM-04 | Broadcast сообщение всем игрокам | P3 |

### 2.4 Алертинг

| ID | Требование | Приоритет |
|----|------------|-----------|
| F-ALT-01 | Оповещение при server offline (Telegram/Email) | P1 |
| F-ALT-02 | Оповещение при high tick latency (>20ms avg) | P2 |
| F-ALT-03 | Оповещение при low memory (<10% free) | P2 |
| F-ALT-04 | Настройка порогов алертов | P2 |

---

## 3. Нефункциональные требования

### 3.1 Безопасность

| ID | Требование | Приоритет |
|----|------------|-----------|
| NF-SEC-01 | HTTPS обязателен | P0 |
| NF-SEC-02 | Аутентификация (login + password или OAuth) | P0 |
| NF-SEC-03 | 2FA для административных действий | P1 |
| NF-SEC-04 | Rate limiting на API | P0 |
| NF-SEC-05 | IP whitelist (опционально) | P2 |
| NF-SEC-06 | JWT токены с коротким TTL (15 мин) + refresh | P0 |
| NF-SEC-07 | Audit log всех действий с IP и timestamp | P0 |

### 3.2 Доступность

| ID | Требование | Приоритет |
|----|------------|-----------|
| NF-AV-01 | Responsive design (desktop + mobile) | P0 |
| NF-AV-02 | PWA (установка на телефон) | P2 |
| NF-AV-03 | Работа при медленном соединении (graceful degradation) | P1 |
| NF-AV-04 | Минимальная ширина экрана: 320px | P0 |

### 3.3 Производительность

| ID | Требование | Приоритет |
|----|------------|-----------|
| NF-PERF-01 | First Contentful Paint < 2 сек | P1 |
| NF-PERF-02 | Дашборд не должен нагружать production сервер | P0 |
| NF-PERF-03 | Кеширование статики (assets) | P1 |

---

## 4. Архитектура

### 4.1 Компоненты

```
┌─────────────────────────────────────────────────────────────┐
│                        КЛИЕНТ                               │
│  ┌─────────────────────────────────────────────────────┐    │
│  │           Web Dashboard (React/Vue/Svelte)          │    │
│  │  • Desktop & Mobile responsive                      │    │
│  │  • PWA-ready                                        │    │
│  └─────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
                              │
                              │ HTTPS
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    MONITORING API                           │
│  ┌─────────────────────────────────────────────────────┐    │
│  │              Node.js / Express                      │    │
│  │  • Auth endpoints (login, refresh, logout)          │    │
│  │  • Metrics endpoints (health, stats, rooms)         │    │
│  │  • Admin endpoints (restart, broadcast)             │    │
│  │  • WebSocket для real-time updates                  │    │
│  └─────────────────────────────────────────────────────┘    │
│                              │                              │
│          ┌───────────────────┼───────────────────┐          │
│          ▼                   ▼                   ▼          │
│  ┌──────────────┐   ┌──────────────┐   ┌──────────────┐    │
│  │   SQLite     │   │    Redis     │   │   Docker     │    │
│  │  (users,     │   │   (cache,    │   │    API       │    │
│  │  audit log)  │   │   sessions)  │   │  (stats)     │    │
│  └──────────────┘   └──────────────┘   └──────────────┘    │
└─────────────────────────────────────────────────────────────┘
                              │
                              │ Internal Network
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                  GAME SERVERS (targets)                     │
│  ┌─────────────────┐    ┌─────────────────┐                │
│  │   MetaServer    │    │   MatchServer   │                │
│  │   :3000         │    │   :2567         │                │
│  │   /health       │    │   /colyseus     │                │
│  │   /api/v1/*     │    │   WebSocket     │                │
│  └─────────────────┘    └─────────────────┘                │
└─────────────────────────────────────────────────────────────┘
```

### 4.2 Источники данных

| Источник | Метод получения | Данные |
|----------|-----------------|--------|
| MetaServer /health | HTTP GET, poll 5s | DB status, Redis status |
| Docker API | Unix socket / TCP | Container stats, logs |
| MatchServer /colyseus | HTTP GET | Rooms, players |
| ArenaRoom logs | Docker logs stream | Tick metrics, events |
| Nginx logs | File tail | Access/error logs |

### 4.3 Варианты деплоя

**Вариант A: Отдельный сервер**
- Monitoring API на отдельном VPS
- Подключается к game server по internal network
- Плюсы: изоляция, не влияет на production
- Минусы: дополнительные расходы

**Вариант B: Sidecar в том же Docker**
- Monitoring API как отдельный контейнер в docker-compose
- Общая сеть с game server
- Плюсы: простота, одна машина
- Минусы: разделяет ресурсы с production

**Рекомендация:** Вариант B для MVP, Вариант A при масштабировании.

---

## 5. API Design

### 5.1 Auth endpoints

```
POST /api/auth/login
  Body: { username, password }
  Response: { accessToken, refreshToken, expiresIn }

POST /api/auth/refresh
  Body: { refreshToken }
  Response: { accessToken, expiresIn }

POST /api/auth/logout
  Headers: Authorization: Bearer <token>
  Response: { success: true }
```

### 5.2 Metrics endpoints

```
GET /api/metrics/health
  Response: {
    servers: [{
      name: "slime-arena",
      status: "healthy",
      database: "connected",
      redis: "connected",
      uptime: 86400,
      lastCheck: "2026-02-03T12:00:00Z"
    }]
  }

GET /api/metrics/stats
  Response: {
    container: {
      cpuPercent: 15.5,
      memoryUsage: 163000000,
      memoryLimit: 4000000000,
      networkRx: 542000000,
      networkTx: 3980000000
    },
    game: {
      activeRooms: 2,
      totalPlayers: 22,
      avgTickLatency: 1.8,
      maxTickLatency: 5.0
    }
  }

GET /api/metrics/rooms
  Response: {
    rooms: [{
      roomId: "abc123",
      name: "arena",
      players: 17,
      phase: "Hunt",
      tick: 2400,
      createdAt: "2026-02-03T11:30:00Z"
    }]
  }

GET /api/metrics/history?period=1h&metric=tickLatency
  Response: {
    points: [
      { timestamp: "...", value: 1.5 },
      ...
    ]
  }
```

### 5.3 Admin endpoints

```
POST /api/admin/restart
  Headers: Authorization: Bearer <token>, X-2FA-Code: 123456
  Body: { containerId: "slime-arena" }
  Response: { success: true, message: "Container restarting" }

GET /api/admin/logs?lines=100&level=error
  Response: {
    logs: [
      { timestamp: "...", level: "error", message: "..." }
    ]
  }

GET /api/admin/audit?limit=50
  Response: {
    actions: [{
      timestamp: "...",
      user: "admin",
      action: "restart",
      target: "slime-arena",
      ip: "1.2.3.4"
    }]
  }
```

### 5.4 WebSocket

```
WS /api/ws

Events (server → client):
  { type: "health", data: {...} }      // каждые 5 сек
  { type: "stats", data: {...} }       // каждые 5 сек
  { type: "log", data: {...} }         // real-time logs
  { type: "alert", data: {...} }       // alerts
```

---

## 6. UI/UX Requirements

### 6.1 Wireframes

**Dashboard (Desktop)**
```
┌─────────────────────────────────────────────────────────────┐
│  🟢 Slime Arena Server Monitor              [Admin ▼] [🔔]  │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │   STATUS     │  │     CPU      │  │    MEMORY    │      │
│  │   🟢 Healthy │  │    17.5%     │  │  163/3820 MB │      │
│  │   Uptime: 2d │  │   ▁▂▃▄▅▆▇█   │  │  ████░░░░░░  │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
│                                                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │    ROOMS     │  │   PLAYERS    │  │ TICK LATENCY │      │
│  │      2       │  │     22       │  │   1.8 ms     │      │
│  │   active     │  │   online     │  │   avg        │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Recent Events                              [Filter] │   │
│  ├─────────────────────────────────────────────────────┤   │
│  │  12:15:32  INFO   Player abc123 joined room xyz     │   │
│  │  12:15:30  INFO   Match started in room xyz         │   │
│  │  12:14:55  WARN   Tick latency spike: 12ms          │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Active Rooms                                        │   │
│  ├─────────────────────────────────────────────────────┤   │
│  │  Room ID      Players   Phase    Tick    Actions    │   │
│  │  V6Y20pdOf    17/20     Hunt     2400    [Details]  │   │
│  │  fAWbj08Ou    5/20      Growth   600     [Details]  │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│                              [🔄 Restart Server]            │
└─────────────────────────────────────────────────────────────┘
```

**Dashboard (Mobile)**
```
┌─────────────────────┐
│ 🟢 Server Monitor   │
│              [☰]    │
├─────────────────────┤
│                     │
│  ┌───────────────┐  │
│  │ 🟢 Healthy    │  │
│  │ Uptime: 2d    │  │
│  └───────────────┘  │
│                     │
│  ┌──────┐ ┌──────┐  │
│  │ CPU  │ │ RAM  │  │
│  │17.5% │ │163MB │  │
│  └──────┘ └──────┘  │
│                     │
│  ┌──────┐ ┌──────┐  │
│  │Rooms │ │Players│ │
│  │  2   │ │  22   │  │
│  └──────┘ └──────┘  │
│                     │
│  [View Logs]        │
│  [View Rooms]       │
│                     │
│  [🔄 Restart]       │
└─────────────────────┘
```

### 6.2 Цветовая схема статусов

| Статус | Цвет | Hex |
|--------|------|-----|
| Healthy | Зелёный | #22C55E |
| Degraded | Жёлтый | #F59E0B |
| Offline | Красный | #EF4444 |
| Unknown | Серый | #6B7280 |

---

## 7. Безопасность (детально)

### 7.1 Аутентификация

```
┌─────────────────────────────────────────────────────────────┐
│                    AUTHENTICATION FLOW                      │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  1. Login                                                   │
│     User → [username + password] → API                      │
│     API → bcrypt.compare(password, hash)                    │
│     API → [accessToken (15min), refreshToken (7d)]          │
│                                                             │
│  2. Request                                                 │
│     User → [Authorization: Bearer <accessToken>] → API      │
│     API → jwt.verify(token, secret)                         │
│                                                             │
│  3. Refresh                                                 │
│     User → [refreshToken] → API                             │
│     API → check refreshToken in DB                          │
│     API → [new accessToken]                                 │
│                                                             │
│  4. Admin Action (restart, etc.)                            │
│     User → [accessToken + X-2FA-Code] → API                 │
│     API → verify 2FA code (TOTP)                            │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 7.2 Хранение секретов

| Секрет | Где хранить |
|--------|-------------|
| JWT_SECRET | ENV variable |
| User passwords | bcrypt hash в SQLite |
| 2FA secrets | Encrypted в SQLite |
| Refresh tokens | SQLite (с expiry) |

### 7.3 Rate Limiting

| Endpoint | Лимит |
|----------|-------|
| POST /auth/login | 5 req/min per IP |
| POST /admin/* | 10 req/min per user |
| GET /metrics/* | 60 req/min per user |
| WS connections | 2 per user |

---

## 8. Этапы разработки

### Phase 1: MVP (P0) — 2-3 дня

- [ ] Auth: login/logout, JWT
- [ ] Dashboard: status, CPU, RAM, rooms count
- [ ] Health polling (MetaServer /health)
- [ ] Docker stats integration
- [ ] Restart button with confirmation
- [ ] Audit log
- [ ] Responsive UI (mobile-friendly)

### Phase 2: Enhanced (P1) — 2-3 дня

- [ ] Logs viewer with filtering
- [ ] Rooms list with details
- [ ] Tick latency metrics
- [ ] 2FA for admin actions
- [ ] Basic alerting (Telegram webhook)

### Phase 3: Advanced (P2) — 3-5 дней

- [ ] Metrics history + charts
- [ ] PWA support
- [ ] IP whitelist
- [ ] Alert thresholds configuration
- [ ] API stats (RPS, latency)

---

## 9. Технологический стек (рекомендация)

### Backend (Monitoring API)

| Компонент | Выбор | Обоснование |
|-----------|-------|-------------|
| Runtime | Node.js 20 | Консистентность со стеком проекта |
| Framework | Express или Fastify | Простота, скорость разработки |
| Auth | jsonwebtoken + bcrypt | Стандартные решения |
| 2FA | speakeasy (TOTP) | Популярная библиотека |
| DB | SQLite (better-sqlite3) | Простота, один файл, без внешних зависимостей |
| Docker API | dockerode | Node.js клиент для Docker |
| WebSocket | ws | Быстрый, простой |

### Frontend

| Компонент | Выбор | Обоснование |
|-----------|-------|-------------|
| Framework | React или Vue 3 | Популярность, экосистема |
| UI Kit | Tailwind CSS + shadcn/ui | Быстрая разработка, responsive |
| Charts | Chart.js или Recharts | Простота, лёгкость |
| State | Zustand или Pinia | Минималистичные решения |
| Build | Vite | Быстрая сборка |

---

## 10. Глоссарий

| Термин | Определение |
|--------|-------------|
| **MetaServer** | REST API сервер для auth, profile, leaderboard (порт 3000) |
| **MatchServer** | Colyseus WebSocket сервер для игровых комнат (порт 2567) |
| **Tick** | Единица игрового времени (33.3ms при 30 FPS симуляции) |
| **Tick Latency** | Время выполнения одного тика симуляции |
| **Room** | Игровая комната Colyseus (матч) |
| **Phase** | Фаза матча: Growth → Hunt → Final → Results |
| **Health Check** | Проверка доступности и состояния сервиса |
| **2FA** | Two-Factor Authentication (двухфакторная аутентификация) |
| **TOTP** | Time-based One-Time Password (алгоритм для 2FA) |
| **Audit Log** | Журнал административных действий |

---

## 11. Риски и митигация

| Риск | Вероятность | Влияние | Митигация |
|------|-------------|---------|-----------|
| Мониторинг нагружает production | Средняя | Высокое | Rate limiting, кеширование, async polling |
| Утечка credentials | Низкая | Критическое | HTTPS, короткий TTL токенов, 2FA |
| Docker API недоступен | Низкая | Высокое | Fallback на shell exec, алерт |
| Неавторизованный рестарт | Низкая | Высокое | 2FA, audit log, IP whitelist |

---

## 12. Открытые вопросы

1. **Где хостить Monitoring API?** Отдельный сервер или sidecar?
2. **Какой frontend framework предпочтителен?** React / Vue / Svelte?
3. **Нужен ли Telegram бот для алертов?** Или достаточно webhook?
4. **Сколько пользователей будет иметь доступ?** Влияет на выбор auth решения.

---

## 13. Ссылки

- [docs/operations/SERVER_SETUP.md](../operations/SERVER_SETUP.md) — текущая настройка сервера
- [server/src/meta/routes/healthRoutes.ts](../../server/src/meta/routes/healthRoutes.ts) — health endpoint
- [server/src/rooms/ArenaRoom.ts](../../server/src/rooms/ArenaRoom.ts) — tick metrics logging
- [@colyseus/monitor](https://docs.colyseus.io/tools/monitor/) — встроенный мониторинг Colyseus
