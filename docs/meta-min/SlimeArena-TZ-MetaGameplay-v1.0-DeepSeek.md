# Техническое задание: Добавление минимального мета-геймплея в Slime Arena

**Версия:** 1.0  
**Дата:** 22 января 2026  
**Автор:** DeepSeek 3.2
**Статус:** Черновик  
**Цель:** Добавить базовую систему профилей, рейтинга и авторизации для софт-лонча  
**Источники:**  

- TECHNICAL_SNAPSHOT_v0.6.0.md  
- GDD-UI.md  
- SlimeArena-Architecture-v4.2.5-Part1..4.md  
- TZ-SoftLaunch-v1.4.7.md  

---

## 1. Обзор изменений

### 1.1. Цель
Добавить минимальный мета-геймплей для удержания игроков:
- Гостевой режим с возможностью регистрации после успешной игры
- Простая система рейтинга (накопительная сумма массы)
- Базовые профили игроков (ник, скин, рейтинг)
- Авторизация через популярные провайдеры (Google, Яндекс)

### 1.2. Принципы
1. **Ненавязчивость:** Новые игроки начинают сразу играть без обязательной регистрации
2. **Мотивация:** Регистрация предлагается после успешного игрового результата
3. **Простота:** Рейтинг рассчитывается как простая сумма массы за все матчи
4. **Совместимость:** Интеграция с существующей архитектурой MetaServer

---

## 2. Пользовательский сценарий

### 2.1. Новый игрок (первый вход)
```
1. Запуск игры → BootScreen
2. Автоматический вход как гость (без регистрации)
3. LobbyScreen с элементами:
   - Случайный ник (например, "БыстрыйСлайм42")
   - Случайный скин (из пула стартовых скинов)
   - Предвыбранный класс (Охотник)
   - Кнопка "Арена" (главное действие)
4. Игрок нажимает "Арена" → MatchmakingScreen → BattleScreen
5. Завершение матча → ResultsScreen
```

### 2.2. Предложение регистрации (после успешной игры)
Условия показа предложения регистрации:
- Игрок достиг массы ≥ 300 кг (уровень 3)
- Игрок занял место в топ-5 матча
- Это первый раз, когда игрок видит предложение

На `ResultsScreen` добавляется баннер:
```
"Отличный результат! 🎉
Сохраните прогресс и получите уникальный скин!"

[Да, сохранить!]   [Сыграть еще]
```

### 2.3. Процесс регистрации
#### Вариант А: Игрок выбирает "Да, сохранить!"
```
1. RegistrationScreen (модальное окно поверх ResultsScreen)
2. Варианты авторизации:
   - Google
   - Яндекс
   - Продолжить как гость (отказ от регистрации)
3. После успешной авторизации:
   - Сохранение текущего ника (с возможностью редактирования)
   - Фиксация текущего скина как "первого и постоянного"
   - Перенос рейтинга (масса из текущего матча)
   - Возврат в LobbyScreen как зарегистрированный пользователь
```

#### Вариант Б: Игрок выбирает "Сыграть еще"
```
1. Возврат в LobbyScreen
2. Возможность сменить класс/имя
3. Следующее предложение регистрации через 3 матча
```

### 2.4. Зарегистрированный игрок
```
1. Автоматическая авторизация при запуске (по токену)
2. LobbyScreen с:
   - Личным ником и скином
   - Текущим рейтингом (общая сумма массы)
   - Кнопкой входа в углу (для смены аккаунта)
3. Каждый завершенный матч увеличивает рейтинг
```

---

## 3. Системные требования

### 3.1. Расширение схемы БД (PostgreSQL)
Дополнения к существующей схеме (Архитектура v4.2.5, Приложение B):

#### Таблица `guests` (временные гостевые аккаунты)
```sql
CREATE TABLE guests (
    guest_id UUID PRIMARY KEY,
    nickname VARCHAR(50) NOT NULL,
    skin_id VARCHAR(100) NOT NULL,
    selected_class VARCHAR(20) DEFAULT 'hunter',
    created_at TIMESTAMP DEFAULT NOW(),
    last_activity TIMESTAMP DEFAULT NOW(),
    registration_offered BOOLEAN DEFAULT FALSE
);

-- Индекс для очистки неактивных гостей
CREATE INDEX idx_guests_last_activity ON guests(last_activity);
```

#### Модификация таблицы `users`
Добавить поле:
```sql
ALTER TABLE users ADD COLUMN is_guest_converted BOOLEAN DEFAULT FALSE;
ALTER TABLE users ADD COLUMN original_guest_id UUID REFERENCES guests(guest_id);
```

#### Новая таблица `simple_ratings`
```sql
CREATE TABLE simple_ratings (
    user_id UUID PRIMARY KEY REFERENCES users(id),
    total_mass DECIMAL(10,2) DEFAULT 0,
    matches_played INT DEFAULT 0,
    best_mass DECIMAL(10,2) DEFAULT 0,
    updated_at TIMESTAMP DEFAULT NOW()
);
```

### 3.2. Новые HTTP API endpoints
Расширение существующего API (Архитектура v4.2.5, Приложение C):

#### POST `/api/v1/auth/guest`
Создание гостевого аккаунта.
```typescript
Request: {
  platformType: "guest"
}

Response: {
  accessToken: string,
  userId: string,  // guest_id
  nickname: string,
  skinId: string,
  profile: ProfileSummary
}
```

#### POST `/api/v1/auth/register`
Конвертация гостя в зарегистрированного пользователя.
```typescript
Request: {
  guestId: string,
  provider: "google" | "yandex" | "email",
  authToken: string,  // OAuth token или email/password
  nickname?: string,  // Опциональное изменение ника
  operationId: string
}

Response: {
  accessToken: string,
  userId: string,  // Новый user_id
  profile: ProfileSummary,
  rating: SimpleRating
}
```

#### GET `/api/v1/profile/rating`
Получение простого рейтинга.
```typescript
Response: {
  totalMass: number,
  matchesPlayed: number,
  bestMass: number,
  globalRank: number  // Позиция в общем рейтинге
}
```

#### GET `/api/v1/leaderboards/simple`
Упрощенный лидерборд.
```typescript
Query: {
  page?: number = 1,
  limit?: number = 50
}

Response: {
  entries: Array<{
    rank: number,
    nickname: string,
    totalMass: number,
    matchesPlayed: number,
    skinId: string
  }>,
  self: {
    rank: number,
    totalMass: number
  }
}
```

### 3.3. Изменения в MetaServer
Новые сервисы:
1. `GuestService` - управление гостевыми аккаунтами
2. `SimpleRatingService` - расчет и обновление простого рейтинга
3. `RegistrationService` - обработка конвертации гостей

Модификации существующих сервисов:
- `AuthService`: добавление гостевой авторизации
- `MatchResultService`: обновление рейтинга после матча

---

## 4. Изменения UI/UX

### 4.1. Новые экраны/компоненты

#### RegistrationScreen (модальное окно)
**Расположение:** Поверх ResultsScreen  
**Элементы:**
```
Заголовок: "Сохранить прогресс"
Текст: "Зарегистрируйтесь, чтобы сохранить свой результат и получить уникальный скин!"

Варианты авторизации:
[ ] Google    [иконка Google]
[ ] Яндекс    [иконка Яндекса]

Поле для ника: [текущий_ник] (редактируемое)

Кнопки:
[Продолжить как гость]   [Зарегистрироваться]
```

#### LobbyScreen - изменения
**Добавить в верхний правый угол:**
- Аватар (скин игрока)
- Ник (с возможностью нажатия для быстрой регистрации если гость)
- Кнопка "Войти" (только для гостей, ведет к RegistrationScreen)
- Кнопка "Выйти" (для зарегистрированных, симметрично "На главный")

**Центральная область:**
- Отображение текущего рейтинга (общая сумма массы)
- Кнопка "Арена" остается основной CTA

### 4.2. ResultsScreen - изменения
**Добавить баннер регистрации:**
Условия показа:
1. Масса ≥ 300 кг
2. Место в топ-5
3. Игрок - гость
4. Еще не предлагалась регистрация

```
Баннер над таблицей результатов:

"🎉 Отличная игра! 
Вы набрали [масса] кг и заняли [место] место!

Сохраните прогресс и получите уникальный скин!"

[Да, сохранить!]   [Сыграть еще]
```

### 4.3. BootScreen - изменения
**Логика определения пользователя:**
1. Проверить наличие сохраненного токена
2. Если токен есть → авторизация
3. Если токена нет → автоматический гостевой вход

---

## 5. Рейтинговая система

### 5.1. Формула расчета
```
Рейтинг = Σ(масса_в_конце_матча) за все матчи
```
Где:
- `масса_в_конце_матча` = масса игрока при завершении матча
- Учитываются только завершенные матчи (не досрочные выходы)
- Матчи до регистрации (как гость) не учитываются

### 5.2. Обновление рейтинга
После каждого матча:
```sql
UPDATE simple_ratings 
SET 
  total_mass = total_mass + :final_mass,
  matches_played = matches_played + 1,
  best_mass = GREATEST(best_mass, :final_mass),
  updated_at = NOW()
WHERE user_id = :user_id;
```

### 5.3. Лидерборд
Сортировка по:
1. `total_mass` (по убыванию)
2. `matches_played` (по убыванию, при равенстве суммы)
3. `updated_at` (по возрастанию, при равенстве игр)

---

## 6. Интеграция с существующей системой

### 6.1. Совместимость с MetaServer
1. **Гостевые аккаунты** используют тот же `accessToken` механизм
2. **Регистрация** создает запись в `users` и связывает с `guests`
3. **Рейтинг** хранится отдельно от Glicko-2 (для будущей миграции)

### 6.2. Изменения в MatchResultService
```typescript
// Псевдокод обработки результатов матча
async processMatchResult(matchSummary: MatchSummary) {
  // Существующая логика...
  await this.applyMatchRewards(matchSummary);
  
  // Новая логика: обновление простого рейтинга
  if (player.isRegistered) {
    await this.simpleRatingService.updateRating(
      player.userId,
      player.finalMass
    );
  }
}
```

### 6.3. Миграция данных
Скрипт миграции для существующих данных:
```sql
-- Создание гостевых записей для анонимных игроков
INSERT INTO guests (guest_id, nickname, skin_id, created_at)
SELECT 
  gen_random_uuid(),
  'Игрок_' || substr(md5(random()::text), 1, 6),
  'skin_default_' || (random() * 5)::int,
  NOW()
FROM generate_series(1, 1000);
```

---

## 7. Требования к реализации

### 7.1. Приоритеты
- **P0:** Гостевой режим, базовый рейтинг, предложение регистрации
- **P1:** Авторизация через Google/Яндекс
- **P2:** Расширенный лидерборд, статистика игрока
- **P3:** Система скинов, редактирование профиля

### 7.2. Ограничения
1. **Гостевые аккаунты** очищаются через 30 дней неактивности
2. **Предложение регистрации** показывается максимум 3 раза
3. **Смена ника** после регистрации - 1 раз бесплатно, далее за валюту
4. **Первый скин** фиксируется при регистрации и не меняется

### 7.3. Аналитика событий
Новые события для отслеживания:
- `guest_created` - создан гостевой аккаунт
- `registration_offered` - предложена регистрация
- `registration_completed` - успешная регистрация
- `rating_updated` - обновление рейтинга
- `leaderboard_viewed` - просмотр лидерборда

---

## 8. Тестирование

### 8.1. Smoke-тесты
1. Новый пользователь → гость → матч → предложение регистрации
2. Регистрация через Google → сохранение прогресса
3. Повторный вход → автоматическая авторизация
4. Несколько матчей → корректный расчет рейтинга

### 8.2. Интеграционные тесты
- Конвертация гостя в пользователя с сохранением данных
- Обновление рейтинга после матча (граничные случаи)
- Очистка неактивных гостевых аккаунтов

---

## 9. Глоссарий новых терминов

| Термин               | Описание                                                |
| -------------------- | ------------------------------------------------------- |
| **Гостевой аккаунт** | Временный профиль без регистрации                       |
| **Простой рейтинг**  | Накопительная сумма массы за все матчи                  |
| **Конвертация**      | Преобразование гостя в зарегистрированного пользователя |
| **Уникальный скин**  | Скин, выдаваемый при первой регистрации                 |
| **Ник-нейм**         | Отображаемое имя игрока                                 |

---

## 10. Следующие шаги

1. **Спринт 1:** Базовая гостeвая система + простой рейтинг
2. **Спринт 2:** UI для регистрации + интеграция OAuth
3. **Спринт 3:** Лидерборд + аналитика метрик
4. **Спринт 4:** Оптимизация UX + A/B тесты

---

## Приложение: JSON схема конфигураций

Дополнение к `features.json`:
```json
{
  "featureFlags": {
    "guestMode": {
      "enabled": true,
      "description": "Гостевой режим для новых игроков"
    },
    "simpleRating": {
      "enabled": true,
      "description": "Простая рейтинговая система"
    },
    "registrationOffer": {
      "enabled": true,
      "minMass": 300,
      "minPosition": 5,
      "maxOffers": 3
    }
  }
}
```

Дополнение к `economy.json`:
```json
{
  "grants": [
    {
      "grantId": "first_registration_skin",
      "conditions": {
        "type": "onRegistration"
      },
      "rewards": [
        {
          "type": "skin",
          "itemId": "lucky_beginner",
          "amount": 1
        }
      ]
    }
  ]
}
```

---

*Документ подготовлен на основе анализа текущей архитектуры и требований к мета-геймплею. Все изменения совместимы с существующей системой и могут быть реализованы поэтапно.*