# Slime Arena — ТЗ: Минимальный мета-геймплей

**Версия:** 1.2  
**Дата:** 22 января 2026  
**Автор:** Claude Opus 4.5  
**Приоритет:** P0  
**Зависимости:** Архитектура v4.2.5, TZ-SoftLaunch-v1.4.7

---

## 1. Цель

Добавить минимальный мета-геймплей для удержания игроков:
- Гостевой режим для быстрого входа новых игроков
- Регистрация через OAuth для сохранения прогресса
- Профиль игрока с никнеймом и скином
- Накопительный рейтинг массы
- Таблица лидеров

---

## 2. Типы пользователей

### 2.1. Гость (anonymous)

Пользователь без регистрации. Данные хранятся только в `localStorage` клиента — запись в БД не создаётся.

| Параметр | Значение |
|----------|----------|
| Хранение | `localStorage` (клиент) |
| Никнейм | Случайный из списка |
| Скин | Случайный из базового набора |
| Рейтинг | Не накапливается |
| Сохранение прогресса | Нет |
| Срок жизни | До очистки `localStorage` |

### 2.2. Зарегистрированный игрок

Пользователь, прошедший регистрацию через OAuth или платформу.

| Параметр | Значение |
|----------|----------|
| Хранение | Таблица `users` (БД) |
| `platformType` | `google`, `yandex`, `telegram`, `vk` |
| Никнейм | Выбранный при регистрации |
| Скин | Закреплённый при регистрации |
| Рейтинг | Накапливается |
| Сохранение прогресса | Да |

---

## 3. Пользовательские сценарии

### 3.1. Первый вход нового игрока

1. Игрок открывает игру по ссылке или рекламе.
2. Система определяет платформу через `PlatformManager`.
3. Если платформа — Telegram Mini App: выполняется тихая авторизация.
4. Если платформа — веб-браузер без контекста: создаётся гостевая сессия в `localStorage`.
5. Игрок попадает на экран лобби.
6. Система генерирует случайный никнейм и случайный скин.
7. Система предлагает выбор класса (один уже выбран по умолчанию).
8. Игрок нажимает кнопку «Играть!».

### 3.2. Завершение матча гостем

1. После завершения матча показывается экран результатов.
2. Если результат выше порога — показывается предложение регистрации.
3. Предложение содержит:
   - Похвалу за результат
   - Информацию о награде (скин за регистрацию)
   - Две кнопки: «Сохранить прогресс» и «Сыграть ещё»
4. При выборе «Сыграть ещё»: возврат в лобби.
5. При выборе «Сохранить прогресс»: переход на экран регистрации.

### 3.3. Регистрация

1. Экран регистрации показывает доступные способы входа.
2. Для веб-браузера: Google и Яндекс.
3. Для Telegram Mini App: регистрация уже выполнена (тихая авторизация).
4. После успешной OAuth-авторизации:
   - Создаётся запись в таблице `users`
   - Данные из `localStorage` переносятся в БД
   - Результат матча, вызвавшего регистрацию, записывается в рейтинг
   - Скин закрепляется за игроком
5. Игроку предлагается изменить никнейм (опционально).
6. После регистрации игрок возвращается в лобби.

### 3.4. Повторный вход зарегистрированного игрока

1. При загрузке страницы система проверяет наличие токена в `localStorage`.
2. Если токен валиден: автоматическая авторизация, переход на главный экран (`MainScreen`).
3. Если токен истёк или отсутствует: показывается лобби с гостевым режимом.
4. Кнопка «Войти» в верхнем правом углу лобби позволяет авторизоваться.

### 3.5. Авторизация существующего игрока

1. Игрок нажимает кнопку «Войти» в лобби.
2. Открывается модальное окно с выбором способа входа.
3. После успешной OAuth-авторизации:
   - Загружается профиль игрока
   - Восстанавливается никнейм, скин, рейтинг
4. Игрок попадает на главный экран (`MainScreen`).

---

## 4. Система рейтинга

### 4.1. Правила расчёта

| Параметр | Значение |
|----------|----------|
| Тип рейтинга | Накопительный |
| Единица измерения | Масса (кг) |
| Начальное значение | 0 |
| Начисление | После каждого матча |
| Формула | `totalMass += finalMass` |

### 4.2. Условия начисления

- Рейтинг начисляется только зарегистрированным игрокам.
- `finalMass` берётся из `MatchSummary.playerResults`.
- Начисление выполняется `MetaServer` при обработке результатов матча.
- Гостям рейтинг не начисляется.

### 4.3. Защита от дублирования

Для предотвращения повторного начисления при повторной отправке `MatchSummary` используется таблица `rating_awards` с уникальным ключом `(user_id, match_id)`.

### 4.4. Таблица лидеров

Два режима отображения:

| Режим | Описание | Целевая аудитория |
|-------|----------|-------------------|
| Накопительный (`total`) | Сумма массы за все матчи | Постоянные игроки |
| Рекордный (`best`) | Лучший результат за один матч | Спортивные игроки |

Общие параметры:

| Параметр | Значение |
|----------|----------|
| Сортировка | По убыванию соответствующей метрики |
| При равенстве | По убыванию `matches_played` |
| Отображение | Топ-100 |
| Позиция игрока | Всегда видна |
| Обновление | При каждом запросе |

### 4.5. Риски рекордного рейтинга

Рекордный рейтинг уязвим для «фарма» в условиях низкого онлайна:
- Игроки могут заходить в ночное время без конкурентов
- В прототипе с малым количеством игроков это особенно легко

Возможные меры защиты (для будущих версий):
- Минимальное количество игроков в матче для засчитывания рекорда
- Учёт количества противников в матче
- Отдельные лидерборды по размеру лобби

---

## 5. Изменения в базе данных

Все изменения требуют обновления **Architecture v4.2.5 Part4 (Приложение B)**.

### 5.1. Новая таблица `leaderboard_total_mass`

Накопительный рейтинг — сумма массы за все матчи.

| Поле | Тип | Ограничения | Описание |
|------|-----|-------------|----------|
| `user_id` | UUID | PRIMARY KEY, FK → users.id | Игрок |
| `total_mass` | BIGINT | DEFAULT 0 | Накопленная масса |
| `matches_played` | INT | DEFAULT 0 | Количество матчей |
| `updated_at` | TIMESTAMP | DEFAULT NOW() | Обновление |

Индексы:
- INDEX (`total_mass` DESC, `matches_played` DESC) — построение таблицы лидеров

### 5.2. Новая таблица `leaderboard_best_mass`

Рекордный рейтинг — лучший результат за один матч.

| Поле | Тип | Ограничения | Описание |
|------|-----|-------------|----------|
| `user_id` | UUID | PRIMARY KEY, FK → users.id | Игрок |
| `best_mass` | BIGINT | DEFAULT 0 | Лучший результат |
| `best_match_id` | UUID | FK → match_results.match_id | Матч с лучшим результатом |
| `players_in_match` | INT | | Количество игроков в матче |
| `achieved_at` | TIMESTAMP | | Дата достижения рекорда |
| `updated_at` | TIMESTAMP | DEFAULT NOW() | Обновление |

Индексы:
- INDEX (`best_mass` DESC) — построение таблицы лидеров

### 5.3. Новая таблица `rating_awards`

Защита от повторного начисления рейтинга.

| Поле | Тип | Ограничения | Описание |
|------|-----|-------------|----------|
| `user_id` | UUID | FK → users.id | Игрок |
| `match_id` | UUID | NOT NULL | Матч |
| `mass_awarded` | BIGINT | NOT NULL | Начисленная масса |
| `created_at` | TIMESTAMP | DEFAULT NOW() | Дата начисления |

Ключи:
- PRIMARY KEY (`user_id`, `match_id`) — идемпотентность

### 5.4. Изменения в таблице `users`

Новые значения для поля `platform_type`:
- `google` — OAuth через Google
- `yandex` — OAuth через Яндекс

Новые поля:

| Поле | Тип | Ограничения | Описание |
|------|-----|-------------|----------|
| `registration_skin_id` | VARCHAR(100) | | Скин, полученный при регистрации |
| `registration_match_id` | UUID | | Матч, вызвавший регистрацию |

### 5.5. Новая таблица `oauth_links`

| Поле | Тип | Ограничения | Описание |
|------|-----|-------------|----------|
| `id` | UUID | PRIMARY KEY | Идентификатор |
| `user_id` | UUID | FK → users.id | Игрок |
| `provider` | VARCHAR(50) | NOT NULL | google, yandex |
| `provider_user_id` | VARCHAR(255) | NOT NULL | Идентификатор у провайдера |
| `email` | VARCHAR(255) | | Email |
| `created_at` | TIMESTAMP | DEFAULT NOW() | Создание |

Индексы:
- UNIQUE (`provider`, `provider_user_id`) — запрет дублей

---

## 6. Изменения в HTTP API

Все изменения требуют обновления **Architecture v4.2.5 Part4 (Приложение C)**.

### 6.1. POST `/api/v1/auth/oauth`

Назначение: OAuth-авторизация или регистрация.

Запрос:

| Поле | Тип | Описание |
|------|-----|----------|
| `provider` | string | `google`, `yandex` |
| `code` | string | OAuth-код от провайдера |
| `redirectUri` | string | URI перенаправления |
| `guestData` | object? | Данные гостя для конвертации |

Структура `guestData`:

| Поле | Тип | Описание |
|------|-----|----------|
| `nickname` | string | Никнейм гостя |
| `skinId` | string | Скин гостя |
| `lastMatchId` | string? | Матч для инициализации рейтинга |

Ответ:

| Поле | Тип | Описание |
|------|-----|----------|
| `accessToken` | string | Токен API |
| `userId` | string | UUID |
| `profile` | ProfileSummary | Данные профиля |
| `isNewUser` | boolean | Первая регистрация |

### 6.2. GET `/api/v1/leaderboard`

Назначение: получение таблицы лидеров.

Параметры запроса:

| Параметр | Тип | Описание |
|----------|-----|----------|
| `mode` | string | `total` (накопительный) или `best` (рекордный) |
| `limit` | number | Количество записей (по умолчанию 100, максимум 100) |
| `offset` | number | Смещение (по умолчанию 0) |

Ответ:

| Поле | Тип | Описание |
|------|-----|----------|
| `mode` | string | Режим рейтинга |
| `entries` | array | Список записей |
| `myPosition` | number? | Позиция текущего игрока |
| `myValue` | number? | Значение метрики текущего игрока |

Структура записи для `mode=total`:

| Поле | Тип | Описание |
|------|-----|----------|
| `position` | number | Место в рейтинге |
| `userId` | string | UUID |
| `nickname` | string | Никнейм |
| `skinId` | string | Скин |
| `totalMass` | number | Накопленная масса |
| `matchesPlayed` | number | Количество матчей |

Структура записи для `mode=best`:

| Поле | Тип | Описание |
|------|-----|----------|
| `position` | number | Место в рейтинге |
| `userId` | string | UUID |
| `nickname` | string | Никнейм |
| `skinId` | string | Скин |
| `bestMass` | number | Лучший результат |
| `playersInMatch` | number | Игроков в матче |
| `achievedAt` | string | Дата достижения |

### 6.3. POST `/api/v1/profile/nickname`

Назначение: изменение никнейма при регистрации.

Запрос:

| Поле | Тип | Описание |
|------|-----|----------|
| `nickname` | string | Новый никнейм |
| `operationId` | string | Ключ идемпотентности |

Ответ: обновлённый профиль.

---

## 7. Изменения в клиенте

### 7.1. Новые экраны

| Экран | Назначение |
|-------|------------|
| `RegistrationPromptModal` | Предложение регистрации после удачного матча |
| `OAuthScreen` | Выбор способа входа |
| `NicknameEditModal` | Изменение никнейма при регистрации |

### 7.2. Изменения в экране лобби

| Элемент | Расположение | Описание |
|---------|--------------|----------|
| Кнопка «Войти» | Верхний правый угол | Для гостей, открывает `OAuthScreen` |
| Кнопка «Регистрация» | Верхний правый угол, рядом с «Войти» | Появляется после 2+ матчей гостя |
| Аватар/никнейм | Верхний правый угол | Для зарегистрированных, открывает профиль |
| Поле никнейма | Рядом с выбором класса | Редактирование для гостей |

### 7.3. Изменения в экране результатов

| Элемент | Условие показа | Описание |
|---------|----------------|----------|
| `RegistrationPromptModal` | Гость + масса ≥ 200 кг | Модальное окно с предложением |
| Текущий рейтинг | Зарегистрированный игрок | Показ накопленной массы |
| Прирост рейтинга | Зарегистрированный игрок | +X кг за этот матч |

### 7.4. Новый экран «Таблица лидеров»

| Элемент | Описание |
|---------|----------|
| Заголовок | «Таблица лидеров» |
| Переключатель режимов | Две вкладки: «Накопительный» / «Рекордный» |
| Список записей | Топ-100 игроков выбранного режима |
| Строка текущего игрока | Выделена, всегда видна |
| Кнопка «Закрыть» | Возврат на предыдущий экран |

Поведение переключателя:
- По умолчанию открывается «Накопительный»
- При переключении загружаются данные другого рейтинга
- Позиция игрока пересчитывается для каждого режима

### 7.5. Хранение данных гостя

В `localStorage` хранятся:

| Ключ | Описание |
|------|----------|
| `guest_nickname` | Сгенерированный никнейм |
| `guest_skin_id` | Сгенерированный скин |
| `guest_match_count` | Количество сыгранных матчей |
| `guest_last_match_id` | Идентификатор последнего матча |

---

## 8. Серверная логика

### 8.1. Генерация гостевого никнейма

Формат: `{Прилагательное} {Существительное}`

Примеры:
- `Весёлый Слайм`
- `Быстрый Охотник`
- `Хитрый Собиратель`

Генерация выполняется на клиенте. Алгоритм уже реализован.

### 8.2. Генерация гостевого скина

- Выбор случайного скина из базового набора (`baseSkins` в `config/skins.json`).
- Гарантия отсутствия премиальных скинов.
- Генерация выполняется на клиенте.
- Скин сохраняется в `localStorage`.

### 8.3. Регистрация нового игрока

1. Валидация OAuth-токена у провайдера.
2. Проверка отсутствия существующей привязки в `oauth_links`.
3. Создание записи в `users`.
4. Создание записи в `oauth_links`.
5. Запись `users.registration_skin_id`.
6. Создание записи в `unlocked_items` для скина.
7. Если передан `lastMatchId`:
   - Проверка, что матч существует в `match_results`
   - Создание записи в `total_mass_leaderboard` с результатом матча
   - Создание записи в `rating_awards`
8. Запись в `audit_log`.

### 8.4. Обновление рейтинга после матча

Выполняется в `MetaServer` при обработке `MatchSummary`:

1. Для каждого `playerResult` с непустым `userId`:
2. Проверка отсутствия записи в `rating_awards` для пары `(user_id, match_id)`.
3. Если записи нет:
   - **Накопительный рейтинг:** обновление `leaderboard_total_mass`: `total_mass += finalMass`, `matches_played += 1`
   - **Рекордный рейтинг:** если `finalMass > best_mass`, обновление `leaderboard_best_mass`: `best_mass = finalMass`, `best_match_id = matchId`, `players_in_match = playersCount`, `achieved_at = NOW()`
   - Создание записи в `rating_awards`
   - Запись в `transactions` с `source = 'match'`
4. Если запись есть — пропуск (идемпотентность).

---

## 9. OAuth-интеграция

### 9.1. Google OAuth 2.0

| Параметр | Значение |
|----------|----------|
| Точка авторизации | `https://accounts.google.com/o/oauth2/v2/auth` |
| Точка токена | `https://oauth2.googleapis.com/token` |
| Точка информации о пользователе | `https://www.googleapis.com/oauth2/v2/userinfo` |
| Область доступа | `openid email profile` |

### 9.2. Яндекс OAuth

| Параметр | Значение |
|----------|----------|
| Точка авторизации | `https://oauth.yandex.ru/authorize` |
| Точка токена | `https://oauth.yandex.ru/token` |
| Точка информации о пользователе | `https://login.yandex.ru/info` |
| Область доступа | `login:info login:email` |

### 9.3. Конфигурация

OAuth-ключи хранятся в переменных окружения:

| Переменная | Описание |
|------------|----------|
| `GOOGLE_CLIENT_ID` | Идентификатор приложения Google |
| `GOOGLE_CLIENT_SECRET` | Секрет приложения Google |
| `YANDEX_CLIENT_ID` | Идентификатор приложения Яндекс |
| `YANDEX_CLIENT_SECRET` | Секрет приложения Яндекс |

---

## 10. Конфигурации

### 10.1. Изменения в `config/skins.json`

Новые поля:

| Поле | Тип | Описание |
|------|-----|----------|
| `baseSkins` | string[] | Скины для гостей |
| `registrationBonus` | boolean | Выдаётся ли при регистрации |

### 10.2. Новые поля в `config/features.json`

| Флаг | Описание |
|------|----------|
| `oauthGoogleEnabled` | Разрешена авторизация через Google |
| `oauthYandexEnabled` | Разрешена авторизация через Яндекс |
| `registrationPromptEnabled` | Показывать предложение регистрации |
| `registrationPromptMinMass` | Минимальная масса для показа (200) |

---

## 11. Логика предложения регистрации

### 11.1. Предложение после удачного матча

| Параметр | Значение |
|----------|----------|
| Минимальная масса | 200 кг |
| Частота показа | После каждого удачного матча |

Логика: `finalMass >= registrationPromptMinMass AND isGuest`

Показывается модальное окно `RegistrationPromptModal` с похвалой и предложением сохранить прогресс.

### 11.2. Кнопка «Регистрация» в лобби

После второго матча гостя (независимо от результата) в лобби рядом с кнопкой «Войти» появляется кнопка «Регистрация».

| Параметр | Значение |
|----------|----------|
| Условие появления | `matchCount >= 2 AND isGuest` |
| Расположение | Верхний правый угол, рядом с «Войти» |
| Действие | Открывает `OAuthScreen` |

Это позволяет удержать игроков, которые хотят зарегистрироваться, но не достигают порога 200 кг.

---

## 12. Требования к безопасности

1. OAuth-токены провайдеров никогда не хранятся на клиенте.
2. `accessToken` имеет ограниченный срок действия (24 часа).
3. Валидация OAuth-токена выполняется только на сервере.
4. Защита от повторной регистрации по одному OAuth-аккаунту через UNIQUE в `oauth_links`.
5. Защита от повторного начисления рейтинга через UNIQUE в `rating_awards`.
6. Валидация `lastMatchId` при регистрации — матч должен существовать в `match_results`.

---

## 13. События аналитики

Новые события для отслеживания воронки конверсии:

| Событие | Описание | Параметры |
|---------|----------|-----------|
| `guest_created` | Создана гостевая сессия | `skinId`, `nickname` |
| `guest_match_completed` | Гость завершил матч | `matchId`, `finalMass` |
| `registration_prompt_shown` | Показано предложение регистрации | `matchId`, `finalMass` |
| `registration_prompt_accepted` | Нажато «Сохранить прогресс» | `matchId` |
| `registration_prompt_declined` | Нажато «Сыграть ещё» | `matchId` |
| `registration_completed` | Успешная регистрация | `provider`, `initialMass` |
| `leaderboard_viewed` | Просмотр таблицы лидеров | `myPosition` |

---

## 14. Приоритеты реализации

### P0 — Обязательно для запуска

- Гостевой режим (хранение в `localStorage`)
- Регистрация через Google и Яндекс
- Накопительный рейтинг массы (`leaderboard_total_mass`)
- Таблица лидеров накопительного рейтинга (топ-100)

### P1 — Желательно для запуска

- Рекордный рейтинг (`leaderboard_best_mass`)
- Переключатель режимов на экране лидеров
- События аналитики
- Кнопка «Регистрация» после 2+ матчей

### P2 — После запуска

- Меры защиты от «фарма» рекордов
- Альтернативные провайдеры OAuth (VK, OK)
- Расширенная статистика профиля
- Недельные/месячные лидерборды

---

## 15. Критерии приёмки

### 15.1. Гостевой режим

- [ ] Новый игрок попадает в лобби без авторизации
- [ ] Генерируется случайный никнейм
- [ ] Генерируется случайный скин из базового набора
- [ ] Данные гостя сохраняются в `localStorage`
- [ ] Гость может играть матчи
- [ ] Рейтинг гостя не накапливается

### 15.2. Регистрация

- [ ] После удачного матча (≥200 кг) гостя показывается предложение регистрации
- [ ] После 2+ матчей в лобби появляется кнопка «Регистрация»
- [ ] Регистрация через Google работает
- [ ] Регистрация через Яндекс работает
- [ ] Скин закрепляется за игроком
- [ ] Результат матча записывается в рейтинг
- [ ] Никнейм можно изменить при регистрации

### 15.3. Авторизация

- [ ] Кнопка «Войти» видна гостям в лобби
- [ ] Повторный вход ведёт на главный экран (`MainScreen`)
- [ ] При истечении токена требуется повторная авторизация

### 15.4. Рейтинг

- [ ] После каждого матча накопительный рейтинг зарегистрированного игрока увеличивается
- [ ] При новом рекорде обновляется рекордный рейтинг
- [ ] Повторная отправка `MatchSummary` не дублирует начисление
- [ ] Таблица лидеров показывает топ-100
- [ ] Переключатель режимов работает корректно
- [ ] Позиция текущего игрока всегда видна в обоих режимах

---

## 16. Обязательные изменения документации

После реализации необходимо обновить следующие документы:

### 16.1. Architecture v4.2.5 Part4

| Приложение | Изменения |
|------------|-----------|
| Приложение B (БД) | Новые таблицы: `leaderboard_total_mass`, `leaderboard_best_mass`, `rating_awards`, `oauth_links`. Новые поля в `users`. |
| Приложение C (API) | Новые маршруты: `/auth/oauth`, `/leaderboard`, `/profile/nickname`. |
| Приложение D (События) | Новые события аналитики (раздел 13). |

### 16.2. GDD-UI.md

| Раздел | Изменения |
|--------|-----------|
| Экран лобби | Кнопки «Войти» и «Регистрация» в верхнем правом углу. |
| Экран результатов | Модальное окно `RegistrationPromptModal`. |
| Новый экран | Описание `LeaderboardScreen` с переключателем режимов. |
| Новый экран | Описание `OAuthScreen`. |

### 16.3. GDD-Core.md

| Раздел | Изменения |
|--------|-----------|
| Мета-прогресс | Описание накопительного и рекордного рейтингов. |
| Типы игроков | Гость vs Зарегистрированный. |

### 16.4. TZ-SoftLaunch

| Раздел | Изменения |
|--------|-----------|
| Smoke-тесты | Добавить тесты авторизации и лидерборда. |
| Критерии приёмки | Добавить критерии из раздела 15. |

Изменения без обновления указанных документов считаются невалидными.

---

## Глоссарий

| Термин | Описание |
|--------|----------|
| `accessToken` | Токен для авторизации запросов к API |
| `best_mass` | Лучший результат массы за один матч |
| `finalMass` | Масса слайма в конце матча |
| `guestData` | Данные гостя для передачи при регистрации |
| `leaderboard_best_mass` | Таблица рекордного рейтинга |
| `leaderboard_total_mass` | Таблица накопительного рейтинга |
| `localStorage` | Локальное хранилище браузера |
| `MainScreen` | Главный экран игры для зарегистрированных игроков |
| `MatchSummary` | Итоговые данные матча |
| OAuth | Протокол авторизации через сторонние сервисы |
| `PlatformManager` | Менеджер определения платформы |
| `platformType` | Тип платформы авторизации |
| `ProfileSummary` | Сводные данные профиля игрока |
| `rating_awards` | Таблица для защиты от дубликатов начисления |
| `skinId` | Идентификатор скина персонажа |
| `total_mass` | Накопленная масса (накопительный рейтинг) |
| UUID | Универсальный уникальный идентификатор |

---

## История изменений

### Версия 1.2 (22 января 2026)
- Разделение рейтинга на две таблицы: `leaderboard_total_mass` и `leaderboard_best_mass`
- Добавлен переключатель режимов на экране лидеров
- Добавлен раздел «Риски рекордного рейтинга» с описанием уязвимости к «фарму»
- Расширен раздел «Обязательные изменения документации» — добавлены GDD-UI, GDD-Core, TZ-SoftLaunch
- Добавлено поле `players_in_match` в таблицу рекордов
- Обновлён API: единый маршрут `/leaderboard` с параметром `mode`

### Версия 1.1 (22 января 2026)
- Добавлена таблица `rating_awards` для защиты от дубликатов начисления
- Добавлен раздел «События аналитики»
- Добавлен раздел «Приоритеты реализации»
- Уточнено хранение данных гостя в `localStorage` (без записи в БД)
- Упрощена структура API: объединены `/auth/oauth` и `/auth/convert`
- Добавлены ключи `localStorage` для гостевых данных
- Расширен глоссарий

### Версия 1.0 (22 января 2026)
- Первоначальная версия документа
