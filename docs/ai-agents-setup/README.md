# AI Agents Setup — Настройка среды разработки с AI-агентами

> **Версия:** 1.0  
> **Дата:** 28 января 2026  
> **Проект:** Slime Arena

Этот пакет содержит все необходимые файлы для настройки среды разработки VS Code с AI-агентами (Claude, Copilot, Gemini) и системой управления задачами Beads.

---

## 📁 Структура пакета

```
ai-agents-setup/
├── README.md                    ← Вы здесь
├── QUICK_START.md               ← Быстрый старт: промпты для активации ролей
├── COPILOT_INSTRUCTIONS.md      ← Инструкции для GitHub Copilot
├── CLAUDE_SETTINGS.md           ← Настройки Claude Code
├── MEMORY_BANK_GUIDE.md         ← Руководство по Memory Bank
│
├── roles/                       ← Описания ролей AI-агентов
│   ├── AGENT_ROLES.md          ← Основной документ ролей (PM, Architect, Developer, Reviewer)
│   ├── PM-ROLE.md              ← Детальное описание роли PM
│   └── ART_DIRECTOR.md         ← Роль Art Director (опционально, для UI/CSS)
│
├── vscode/                      ← Настройки VS Code
│   ├── settings.json           ← Конфигурация редактора
│   └── extensions.json         ← Рекомендуемые расширения
│
└── beads/                       ← Система управления задачами
    └── config.yaml             ← Конфигурация Beads
```

---

## 🚀 Быстрая установка

### Шаг 1: Установить VS Code расширения

Скопируйте `vscode/extensions.json` в `.vscode/` вашего проекта, затем:

```bash
# VS Code предложит установить рекомендуемые расширения автоматически
# Или установите вручную:
code --install-extension anthropic.claude-code
code --install-extension github.copilot
code --install-extension github.copilot-chat
```

### Шаг 2: Настроить VS Code

```bash
# Скопируйте настройки в ваш проект
cp vscode/settings.json .vscode/settings.json
```

### Шаг 3: Настроить Copilot Instructions

```bash
# Создайте папку .github если её нет
mkdir -p .github

# Скопируйте инструкции
cp COPILOT_INSTRUCTIONS.md .github/copilot-instructions.md
```

### Шаг 4: Настроить Claude Code

```bash
# Скопируйте в корень проекта
cp CLAUDE_SETTINGS.md CLAUDE.md
```

### Шаг 5: Настроить роли агентов

```bash
# Создайте папку .beads
mkdir -p .beads/roles

# Скопируйте роли
cp roles/AGENT_ROLES.md .beads/AGENT_ROLES.md
cp roles/ART_DIRECTOR.md .beads/roles/ART_DIRECTOR.md

# Скопируйте конфигурацию Beads
cp beads/config.yaml .beads/config.yaml
```

### Шаг 6: Настроить Memory Bank

```bash
# Создайте структуру Memory Bank
mkdir -p .memory_bank

# Скопируйте руководство
cp MEMORY_BANK_GUIDE.md agents.md

# Создайте базовые файлы (опционально)
touch .memory_bank/projectbrief.md
touch .memory_bank/activeContext.md
touch .memory_bank/progress.md
```

### Шаг 7: Установить Beads CLI

```bash
# Установка через pip
pip install beads-cli

# Или через npm (если доступно)
npm install -g beads-cli

# Инициализация в проекте
bd init
```

---

## 📋 Описание файлов

### Корневые файлы

| Файл | Назначение | Куда копировать |
|------|------------|-----------------|
| `QUICK_START.md` | Промпты для активации ролей агентов | Корень проекта |
| `COPILOT_INSTRUCTIONS.md` | Правила для GitHub Copilot | `.github/copilot-instructions.md` |
| `CLAUDE_SETTINGS.md` | Настройки Claude Code | `CLAUDE.md` (корень) |
| `MEMORY_BANK_GUIDE.md` | Руководство по Memory Bank | `agents.md` (корень) |

### Папка `roles/` — Роли AI-агентов

| Файл | Роль | Описание |
|------|------|----------|
| `AGENT_ROLES.md` | Все роли | Основной документ с PM, Architect, Developer, Reviewer (~789 строк) |
| `PM-ROLE.md` | PM | Детальное описание Project Manager (~948 строк) |
| `ART_DIRECTOR.md` | Art Director | Визуальный дизайн, CSS, UI (опционально) |

### Папка `vscode/` — Настройки VS Code

| Файл | Описание |
|------|----------|
| `settings.json` | Настройки редактора: форматирование, Beads интеграция, exclude patterns |
| `extensions.json` | Список рекомендуемых расширений для AI-разработки |

### Папка `beads/` — Система задач

| Файл | Описание |
|------|----------|
| `config.yaml` | Конфигурация Beads: префикс задач, sync-branch, auto-commit |

---

## 🎭 Роли AI-агентов

### Основные роли (загружаются по умолчанию)

| Роль | Номер | Функция |
|------|-------|---------|
| **PM** | 0️⃣ | Координация, планирование, эскалация, Landing the Plane |
| **Architect** | 1️⃣ | Декомпозиция задач, архитектурные решения |
| **Developer** | 2️⃣ | Написание кода, исправление багов |
| **Reviewer** | 3️⃣ | Code review, проверка качества |

### Опциональные роли (загружаются отдельно)

| Роль | Номер | Файл |
|------|-------|------|
| **Art Director** | 5️⃣ | `.beads/roles/ART_DIRECTOR.md` |

---

## 🔧 Workflow использования

### 1. Активация роли

```
Ты PM. Прочитай AGENT_ROLES.md секция "0️⃣ PM".
Задача: [описание задачи]
```

### 2. Цикл разработки

```
Architect → [декомпозиция] → Developer → [код] → Reviewer → [проверка] → PM
     ↑                                                                    ↓
     └─────────────────────── [issues / scope creep] ─────────────────────┘
```

### 3. Завершение сессии (Landing the Plane)

```bash
git add .
git commit -m "session: описание"
git pull --rebase
git push
bd sync --status
```

---

## 📚 Memory Bank

Memory Bank — структурированная документация для сохранения контекста между сессиями.

### Структура

```
.memory_bank/
├── projectbrief.md      ← Суть проекта
├── productContext.md    ← Бизнес-контекст
├── activeContext.md     ← Текущее состояние (обновлять часто!)
├── systemPatterns.md    ← Архитектурные паттерны
├── techContext.md       ← Технологический стек
└── progress.md          ← Прогресс задач
```

### Правила обновления

1. `activeContext.md` — обновлять при каждом значительном изменении
2. `progress.md` — обновлять при завершении задач
3. Перед началом работы — всегда читать Memory Bank

---

## 🛠 Beads CLI

Beads — AI-native система управления задачами.

### Основные команды

```bash
bd prime              # Загрузить контекст
bd ready              # Показать доступные задачи
bd show <id>          # Детали задачи
bd create "Название"  # Создать задачу
bd update <id> --status=in_progress
bd close <id>         # Закрыть задачу
bd sync               # Синхронизировать с Git
bd stats              # Статистика
```

### Конфигурация

Файл `.beads/config.yaml`:

```yaml
issue-prefix: "your-project"  # Префикс задач
sync-branch: "beads-sync"     # Ветка синхронизации
auto-commit: true             # Автокоммит изменений
auto-push: true               # Автопуш на remote
```

---

## ⚠️ Важные правила

### Для всех агентов

1. **Детерминизм:** Не использовать `Math.random()`, `Date.now()` в игровой логике
2. **Баланс:** Все числовые параметры — в `config/balance.json`
3. **Комментарии:** На русском, идентификаторы — на английском
4. **Commits:** На английском, формат `type: short description`

### Для PM

1. **Landing the Plane:** Работа не завершена, пока `git push` не выполнен
2. **Эскалация:** Opus → Codex → Человек (после 5 неудач)
3. **Не пушить в main:** Только в ветки, merge делает человек

### Для Reviewer

1. **Copilot comments:** Если есть замечания Copilot — обязательно рассмотреть
2. **Вердикты:** APPROVED / CHANGES_REQUESTED / ESCALATION
3. **Не исправлять код:** Возвращать Developer на доработку

---

## 📞 Поддержка

- **Документация Beads:** https://beads.dev/docs
- **Claude Code:** https://claude.ai/code
- **GitHub Copilot:** https://github.com/features/copilot

---

**Автор:** PM Agent  
**Версия:** 1.0  
**Лицензия:** MIT
